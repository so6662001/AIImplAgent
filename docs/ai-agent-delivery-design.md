# 钢铁行业管理软件 — AI智能体辅助交付系统 分析与设计方案

## 目录

- [一、现状分析与痛点识别](#一现状分析与痛点识别)
- [二、AI介入可行性评估矩阵](#二ai介入可行性评估矩阵)
- [三、AI智能体总体架构](#三ai智能体总体架构)
- [四、各智能体详细设计](#四各智能体详细设计)
- [五、智能体间协作流程](#五智能体间协作流程)
- [六、技术架构设计（Java + Vue3）](#六技术架构设计java--vue3)
- [七、数据体系设计](#七数据体系设计)
- [八、实施路径建议](#八实施路径建议)
- [九、风险与应对策略](#九风险与应对策略)

---

## 一、现状分析与痛点识别

### 1.1 现有交付流程全景

```
项目安排 → 客户调研 → 交付计划 → 深度调研 → 模拟演练 → 培训 → 期初数据 → 建帐导数 → 启用帐套 → 跟进辅助 → 成果交付 → 售后跟进
   ①         ②          ③         ④          ⑤        ⑥        ⑦          ⑧          ⑨         ⑩          ⑪          ⑫
```

### 1.2 各环节痛点分析

| 环节 | 当前方式 | 核心痛点 | 耗时占比(估) |
|------|---------|---------|-------------|
| ①项目安排 | 人工调度，凭经验分配PM | 分配不均，难以全局优化 | 2% |
| ②客户调研 | PM电话/现场沟通 | 调研模板不统一，信息遗漏 | 8% |
| ③交付计划 | PM手工制作文档 | 文档格式不一，耗时久 | 5% |
| ④深度调研 | 团队进场，手工整理分析 | 分析维度多、整理工作量大 | 15% |
| ⑤模拟演练 | 手动配置演示环境 | 环境准备慢，场景覆盖不全 | 10% |
| ⑥培训 | 人工培训，重复讲解 | 人力成本极高，效果参差 | 15% |
| ⑦期初数据 | 手工整理、核对 | 数据量大、错误率高 | 10% |
| ⑧建帐导数 | 手工操作 | 操作繁琐、易出错 | 8% |
| ⑨启用帐套 | 人工检查确认 | 检查项多、遗漏风险 | 3% |
| ⑩跟进辅助 | 驻场人员 | 人力成本最高 | 12% |
| ⑪成果交付 | 人工编写报告 | 数据汇总慢、文档工作量大 | 5% |
| ⑫售后跟进 | 人工接线/远程 | 重复问题多、响应慢 | 7% |

### 1.3 关键问题总结

1. **重复性工作占比高**：模板文档生成、数据整理校验、常见问题解答等大量重复劳动
2. **经验难以沉淀**：PM个人经验难以标准化传承
3. **信息孤岛**：各环节信息传递依赖人工，存在断层
4. **质量不稳定**：强依赖个人能力，交付质量波动大
5. **响应效率低**：培训与售后均需人工实时响应

---

## 二、AI介入可行性评估矩阵

### 2.1 评估维度说明

- **AI替代度**：该环节工作可被AI替代的程度（高/中/低）
- **技术成熟度**：当前AI技术实现该能力的成熟度（高/中/低）
- **业务价值**：AI介入后带来的降本增效价值（高/中/低）
- **实施难度**：技术实施与业务适配的难度（高/中/低）

### 2.2 评估矩阵

| 环节 | AI替代度 | 技术成熟度 | 业务价值 | 实施难度 | 优先级 | 建议策略 |
|------|---------|-----------|---------|---------|--------|---------|
| ①项目安排 | 中 | 高 | 中 | 低 | P2 | AI辅助推荐，人工决策 |
| ②客户调研 | 中 | 高 | 高 | 中 | P1 | AI生成调研框架+智能问卷 |
| ③交付计划 | 高 | 高 | 高 | 低 | P0 | AI自动生成，人工审核 |
| ④深度调研 | 中 | 中 | 高 | 中 | P1 | AI辅助分析+报告生成 |
| ⑤模拟演练 | 中 | 中 | 中 | 高 | P2 | AI生成演练场景+环境配置 |
| ⑥培训 | 高 | 高 | 高 | 中 | P0 | AI培训助手+智能问答 |
| ⑦期初数据 | 高 | 高 | 高 | 中 | P0 | AI数据清洗校验 |
| ⑧建帐导数 | 高 | 高 | 高 | 低 | P1 | AI自动化执行 |
| ⑨启用帐套 | 中 | 高 | 中 | 低 | P2 | AI检查清单自动化 |
| ⑩跟进辅助 | 高 | 高 | 高 | 中 | P0 | AI智能客服替代驻场 |
| ⑪成果交付 | 高 | 高 | 中 | 低 | P1 | AI自动生成报告 |
| ⑫售后跟进 | 高 | 高 | 高 | 低 | P0 | AI客服+工单智能流转 |

### 2.3 优先级分组

- **P0（立即启动）**：③交付计划生成、⑥智能培训、⑦数据清洗、⑩智能跟进、⑫智能售后
- **P1（次期推进）**：②调研辅助、④深度分析、⑧自动建帐、⑪报告生成
- **P2（远期规划）**：①智能调度、⑤演练生成、⑨上线检查

---

## 三、AI智能体总体架构

### 3.1 智能体全景图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI 智能体中台（Agent Hub）                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    智能体编排引擎（Orchestrator）                │   │
│  │         负责智能体间的协调调度、上下文传递、流程流转              │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│  │ 项目调度   │ │ 调研分析   │ │ 计划生成   │ │ 背景分析   │          │
│  │ Agent     │ │ Agent     │ │ Agent     │ │ Agent     │          │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│  │ 演练配置   │ │ 培训助手   │ │ 数据治理   │ │ 帐套管理   │          │
│  │ Agent     │ │ Agent     │ │ Agent     │ │ Agent     │          │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘          │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐                        │
│  │ 现场辅助   │ │ 交付报告   │ │ 售后服务   │                        │
│  │ Agent     │ │ Agent     │ │ Agent     │                        │
│  └───────────┘ └───────────┘ └───────────┘                        │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    共享能力层                                  │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │   │
│  │  │ LLM  │ │ RAG  │ │ 知识库│ │文档生成│ │数据分析│ │工作流 │    │   │
│  │  │ 网关  │ │ 引擎  │ │ 管理  │ │ 引擎  │ │ 引擎  │ │ 引擎  │    │   │
│  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 核心设计原则

1. **人机协同（Human-in-the-Loop）**：AI辅助不替代，关键决策必须有人工审核确认节点
2. **知识驱动**：以钢铁行业交付知识库为核心，持续积累沉淀
3. **可编排**：智能体独立运行，也可组合编排成复杂工作流
4. **渐进增强**：从辅助到半自动到自动，逐步提升AI参与度
5. **可观测**：所有AI决策过程可追溯、可审计

---

## 四、各智能体详细设计

### 4.1 项目调度智能体（Project Dispatch Agent）

**对应环节**：① 项目安排，确定项目经理

**核心能力**：
- 基于项目特征（客户规模、行业细分、系统复杂度）和PM画像（技能矩阵、当前负载、历史业绩、客户评价）进行智能匹配
- 输出推荐的PM人选及匹配理由

**输入**：
```
{
  "project_info": {
    "customer_name": "客户名称",
    "industry_segment": "钢贸/钢厂/加工中心/...",
    "estimated_scale": "大/中/小",
    "modules": ["进销存", "财务", "仓储", ...],
    "special_requirements": "特殊需求描述",
    "expected_start_date": "预计启动日期",
    "region": "所在地区"
  }
}
```

**输出**：
```
{
  "recommended_pms": [
    {
      "pm_id": "PM001",
      "name": "张三",
      "match_score": 92,
      "match_reasons": [
        "具有5个钢贸企业交付经验",
        "当前负载率60%，有余量",
        "距离客户所在地较近"
      ],
      "risk_notes": "近期有一个项目即将上线，需关注时间冲突"
    }
  ],
  "schedule_suggestion": {
    "recommended_start": "2026-05-01",
    "estimated_duration": "45天",
    "key_milestones": [...]
  }
}
```

**AI策略**：
- 使用规则引擎 + LLM混合模式
- 硬约束（资质、可用性）用规则过滤，软约束（匹配度）用LLM评分
- 持续学习：项目完成后的评价数据反馈到匹配模型

---

### 4.2 调研分析智能体（Research Agent）

**对应环节**：② 联系客户做项目调研 + ④ 深度调研

**核心能力**：
- 根据客户行业/规模/经营模式，自动生成定制化调研问卷和访谈提纲
- 实时辅助PM进行调研：语音转文字 → 关键信息提取 → 结构化沉淀
- 自动生成客户背景分析报告

**子能力拆分**：

#### 4.2.1 调研问卷生成器

**Prompt模板设计思路**：
```
角色：你是一位钢铁行业管理软件的资深实施顾问
任务：根据以下客户初步信息，生成一份结构化的调研问卷

客户信息：
- 企业类型：{enterprise_type} (钢贸商/钢厂/加工中心/综合服务商)
- 预估规模：{scale}
- 意向模块：{modules}

要求覆盖以下维度：
1. 经营模式分析（直营/代理/混合，内贸/外贸）
2. 经营品类（板材/型材/管材/特钢/不锈钢等，规格/材质/品牌分布）
3. 库存状况（库存量级，仓库数量与分布，库存周转率）
4. 销售状况（月均销售量/金额，定价模式，结算方式）
5. 客户群体（客户数量，客户类型分布，主要大客户）
6. 组织架构（部门设置，人员规模，关键岗位）
7. 现有IT系统（在用系统，痛点，数据情况）
8. 管理诉求与目标

请根据企业类型重点调整问卷侧重点。
```

#### 4.2.2 调研信息结构化引擎

**能力说明**：将PM录入的非结构化调研记录（文字/语音转文字），自动提取并填充到标准化的客户画像模型中。

**客户画像数据模型**：
```
CustomerProfile {
  // 基本信息
  basic_info: {
    company_name, legal_person, registered_capital,
    establishment_date, region, address
  }
  // 经营模式
  business_model: {
    type: "钢贸商|钢厂|加工中心|综合服务商",
    trade_mode: "直营|代理|混合",
    trade_scope: "内贸|外贸|内外贸",
    main_business_description: "..."
  }
  // 经营品类
  product_categories: [
    { category, sub_category, brands[], specifications[], proportion }
  ]
  // 库存状况
  inventory: {
    total_volume, warehouse_count, warehouses[],
    turnover_rate, management_method
  }
  // 销售状况
  sales: {
    monthly_volume, monthly_amount, pricing_model,
    settlement_methods[], credit_policy
  }
  // 客户群体
  customer_base: {
    total_count, customer_types[], top_customers[],
    customer_distribution
  }
  // 组织架构
  organization: {
    departments[], total_staff, key_positions[],
    decision_chain, org_chart_url
  }
  // 现有系统
  existing_systems: [
    { system_name, vendor, modules[], pain_points[], data_status }
  ]
  // 项目目标
  project_goals: {
    management_goals: [],    // 管理目标
    process_goals: [],       // 流程目标
    efficiency_goals: [],    // 效率目标
    risk_control_goals: [],  // 风控目标
    other_goals: []          // 其他目标
  }
}
```

#### 4.2.3 背景分析报告生成器

**能力说明**：基于结构化的客户画像，自动生成完整的《客户背景分析报告》，包含：
- 企业概况与行业定位
- 经营模式分析与建议
- 业务流程梳理（输出标准流程图描述）
- 项目目标总结与优先级建议
- 交付风险预评估
- 项目成果定义建议

---

### 4.3 计划生成智能体（Plan Generator Agent）

**对应环节**：③ 项目交付计划制作

**核心能力**：
- 基于客户画像、项目范围、历史同类项目数据，自动生成项目交付计划
- 包含：里程碑、任务分解（WBS）、资源需求、时间排期、风险预案

**输入**：
```
{
  "customer_profile": { ... },        // 来自调研分析智能体
  "project_scope": {
    "modules": ["进销存", "财务", "仓储"],
    "customization_level": "标准/轻度定制/深度定制",
    "integration_requirements": ["ERP对接", "银行接口"]
  },
  "constraints": {
    "deadline": "2026-08-01",
    "budget": "...",
    "available_resources": [...]
  }
}
```

**输出**：
```
{
  "project_plan": {
    "milestones": [
      { "name": "调研完成", "date": "...", "deliverables": [...] },
      { "name": "模拟演练通过", "date": "...", "deliverables": [...] },
      { "name": "培训完成", "date": "...", "deliverables": [...] },
      { "name": "正式上线", "date": "...", "deliverables": [...] },
      { "name": "交付验收", "date": "...", "deliverables": [...] }
    ],
    "wbs": [...],           // 详细任务分解
    "resource_plan": [...],  // 人员与资源计划
    "risk_plan": [...],      // 风险预案
    "gantt_data": [...]      // 甘特图数据
  }
}
```

**AI策略**：
- RAG检索历史同类项目计划作为参考
- LLM基于参考案例 + 当前项目约束生成新计划
- 内置钢铁行业交付最佳实践知识库
- 自动识别高风险节点并标注

---

### 4.4 模拟演练智能体（Simulation Agent）

**对应环节**：⑤ 模拟演练软件系统

**核心能力**：
- 根据客户业务场景自动生成模拟数据和测试用例
- 自动化场景配置：根据客户品类/流程特点配置演示环境
- 演练偏差检测：对比预期结果与实际演练结果

**核心功能**：

1. **模拟数据生成**
   - 根据客户经营品类，自动生成仿真的产品目录、客户档案、供应商档案
   - 根据库存量级和销售规模生成合理的模拟业务数据
   - 保证数据间的逻辑一致性（例如：库存数 = 入库数 - 出库数）

2. **演练场景脚本生成**
   - 基于客户业务流程，自动编排演练场景脚本
   - 覆盖核心业务路径 + 异常/边界场景
   - 输出step-by-step的操作指南

3. **演练结果评估**
   - 演练过程中自动记录操作日志
   - 与预期结果对比，标记偏差项
   - 生成《模拟演练评估报告》

---

### 4.5 培训助手智能体（Training Agent）

**对应环节**：⑥ 培训

**核心能力**：
- 根据岗位角色自动生成针对性的培训材料
- AI实时问答：培训期间和培训后的即时答疑
- 学习效果评估与个性化补强

**子系统设计**：

#### 4.5.1 培训内容生成引擎

**按角色分层**：
```
┌──────────────────────────────────────────┐
│              培训内容矩阵                  │
├──────────┬──────────┬──────────┬─────────┤
│  角色     │ 内容重点   │ 深度     │ 形式    │
├──────────┼──────────┼──────────┼─────────┤
│ 高层管理  │ 报表/看板  │ 概览     │ 图文简报 │
│ 中层管理  │ 审批/分析  │ 应用级   │ 操作手册 │
│ 业务骨干  │ 全流程操作 │ 精通级   │ 视频+实操│
│ 普通操作  │ 岗位操作   │ 操作级   │ 操作卡片 │
│ IT运维   │ 系统配置   │ 技术级   │ 技术文档 │
└──────────┴──────────┴──────────┴─────────┘
```

**生成内容类型**：
- 操作手册（图文并茂，step-by-step）
- 操作视频脚本（可配合录屏工具自动生成）
- 岗位操作速查卡（一页纸快速参考）
- 考核题库（选择题/操作题/场景题）

#### 4.5.2 智能培训问答Bot

**技术方案**：
- 基于RAG（检索增强生成）架构
- 知识来源：产品文档 + 操作手册 + 历史QA + 行业知识
- 支持多轮对话，理解上下文
- 支持截图识别：用户截图提问，AI识别界面并给出操作指引

**对话示例**：
```
用户：钢卷入库的时候怎么录入炉号和捆号？
Bot：在入库单界面，您可以按以下步骤操作：
     1. 进入【仓储管理】→【入库管理】→【新建入库单】
     2. 选择品类为"钢卷"后，系统会自动显示炉号和捆号字段
     3. 炉号在"炉批号"字段录入，捆号在"件号"字段录入
     4. 如果是批量入库，可以使用"批量导入"功能...
     [附操作截图]
```

#### 4.5.3 学习效果评估

- 自动生成岗位考核试卷
- AI批改（客观题自动批改，操作题基于日志分析评分）
- 生成个人/部门学习报告
- 薄弱环节识别 → 自动推送补充学习材料

---

### 4.6 数据治理智能体（Data Governance Agent）

**对应环节**：⑦ 准备期初数据 + ⑧ 建帐套并导入数据

**核心能力**：
- 智能识别和解析各种格式的源数据（Excel/CSV/数据库导出/纸质扫描/OCR）
- 数据清洗：去重、纠错、标准化、补全
- 数据映射：源数据字段到目标系统字段的智能映射
- 数据校验：业务规则校验、勾稽关系验证、跨表一致性检查
- 帐套配置自动化
- 导入过程异常处理与回滚

---

#### 4.6.1 期初数据全景分类与处理策略

期初数据共涉及 **5大类21项** 数据，AI智能体针对每一项制定专属的处理策略：

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         期初数据全景图                                     │
│                                                                          │
│  ┌─────────────────────────────────────────┐                            │
│  │ A. 基础设置类（搭建帐套骨架，必须最先导入）   │                            │
│  │    ├─ A1. 企业资料                        │                            │
│  │    ├─ A2. 部门架构                        │                            │
│  │    ├─ A3. 员工档案                        │                            │
│  │    ├─ A4. 用户档案                        │                            │
│  │    └─ A5. 账号资料                        │                            │
│  └─────────────────────────────────────────┘                            │
│  ┌─────────────────────────────────────────┐                            │
│  │ B. 业务档案类（支撑业务运转的主数据）        │                            │
│  │    ├─ B1. 客户档案                        │                            │
│  │    ├─ B2. 供应商档案                      │                            │
│  │    ├─ B3. 货品类别                        │                            │
│  │    ├─ B4. 货品方案                        │                            │
│  │    ├─ B5. 仓库档案                        │                            │
│  │    └─ B6. 库位档案                        │                            │
│  └─────────────────────────────────────────┘                            │
│  ┌─────────────────────────────────────────┐                            │
│  │ C. 财务基础类（会计核算体系）               │                            │
│  │    └─ C1. 财务科目                        │                            │
│  └─────────────────────────────────────────┘                            │
│  ┌─────────────────────────────────────────┐                            │
│  │ D. 期初余额类（启用时点的存量数据快照）      │                            │
│  │    ├─ D1. 库存期初数                      │                            │
│  │    ├─ D2. 客户欠款期初数                   │                            │
│  │    ├─ D3. 供应商期初数                     │                            │
│  │    ├─ D4. 账户余额                        │                            │
│  │    ├─ D5. 科目余额                        │                            │
│  │    ├─ D6. 其他应收余额                     │                            │
│  │    └─ D7. 其他应付余额                     │                            │
│  └─────────────────────────────────────────┘                            │
│  ┌─────────────────────────────────────────┐                            │
│  │ E. 税务发票类（税务衔接数据）               │                            │
│  │    ├─ E1. 应开销项发票期初数                │                            │
│  │    └─ E2. 应收进项发票期初数                │                            │
│  └─────────────────────────────────────────┘                            │
└──────────────────────────────────────────────────────────────────────────┘
```

---

#### 4.6.2 导入依赖顺序与编排

期初数据之间存在严格的 **引用依赖关系**，必须按正确顺序导入。AI智能体自动识别并编排导入顺序：

```
导入阶段编排（严格依赖序）:

第1批 ─── 基础设置（无外部依赖）
 │  ├─ A1. 企业资料
 │  ├─ C1. 财务科目
 │  └─ A5. 账号资料（银行/现金账户）
 │
第2批 ─── 组织与人员（依赖企业资料）
 │  ├─ A2. 部门架构        ← 依赖：企业资料
 │  ├─ A3. 员工档案        ← 依赖：部门架构
 │  └─ A4. 用户档案        ← 依赖：员工档案
 │
第3批 ─── 业务档案（依赖基础设置）
 │  ├─ B1. 客户档案        ← 依赖：无（独立主数据）
 │  ├─ B2. 供应商档案      ← 依赖：无（独立主数据）
 │  ├─ B3. 货品类别        ← 依赖：无（独立主数据）
 │  ├─ B4. 货品方案        ← 依赖：货品类别
 │  ├─ B5. 仓库档案        ← 依赖：无（独立主数据）
 │  └─ B6. 库位档案        ← 依赖：仓库档案
 │
第4批 ─── 期初余额（依赖所有档案类数据）
 │  ├─ D1. 库存期初数      ← 依赖：货品方案、仓库、库位
 │  ├─ D2. 客户欠款期初数   ← 依赖：客户档案
 │  ├─ D3. 供应商期初数     ← 依赖：供应商档案
 │  ├─ D4. 账户余额        ← 依赖：账号资料
 │  ├─ D5. 科目余额        ← 依赖：财务科目
 │  ├─ D6. 其他应收余额     ← 依赖：客户档案（或个人）
 │  └─ D7. 其他应付余额     ← 依赖：供应商档案（或个人）
 │
第5批 ─── 税务发票（依赖客户/供应商+货品）
    ├─ E1. 应开销项发票期初数 ← 依赖：客户档案、货品方案
    └─ E2. 应收进项发票期初数 ← 依赖：供应商档案、货品方案
```

**AI编排策略**：
- 同一批次内的数据可并行导入
- 上一批次全部校验通过后才启动下一批次
- 任一数据项导入失败不阻塞同批次其他项，但阻塞下游依赖项
- 自动检测循环依赖并报警

---

#### 4.6.3 各数据项详细处理设计

##### A类：基础设置类数据

**A1. 企业资料**

| 维度 | 说明 |
|------|------|
| 数据来源 | 营业执照扫描件 / 客户手工填写 / 工商信息API |
| AI处理能力 | OCR识别营业执照 → 自动提取企业名称、统一社会信用代码、法人、注册资本、经营范围 |
| 字段清单 | 企业全称、简称、统一社会信用代码、法定代表人、注册资本、成立日期、企业类型、经营范围、注册地址、办公地址、联系电话、传真、邮箱、开户行、银行账号、纳税人识别号、纳税人类型(一般/小规模) |
| AI校验规则 | 统一社会信用代码格式校验(18位)；纳税人识别号格式校验；银行账号与开户行匹配校验 |

```
AI处理流程:
营业执照图片 → OCR识别 → 结构化提取 → 工商信息API交叉验证 → 填充企业资料表 → 人工确认
```

**A2. 部门架构**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户提供的组织架构图（图片/PPT/Word）/ 调研记录 |
| AI处理能力 | 识别组织架构图 → 解析树形层级关系 → 生成标准部门编码 |
| 字段清单 | 部门编码、部门名称、上级部门编码、部门负责人、排序号、启用状态 |
| AI校验规则 | 层级关系完整性（无孤立节点）；部门编码唯一性；根节点存在性 |

```
AI处理能力详述:
1. 从组织架构图（图片格式）中用视觉模型识别层级关系
2. 从Word/PPT的文字描述中用LLM提取部门树
3. 自动生成规范的部门编码（支持多种编码规则：层级编码/流水编码）
4. 识别常见的钢铁企业部门名称并标准化
   例："业务部"/"销售部"/"营销部" → 建议统一为"销售部"
   例："磅房"/"计量部" → 标记为仓储相关部门
```

**A3. 员工档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | Excel花名册 / HR系统导出 / 手工填写 |
| AI处理能力 | 智能字段映射；手机号/身份证格式校验纠错；职位归类标准化 |
| 字段清单 | 员工编号、姓名、性别、身份证号、手机号、邮箱、入职日期、所属部门、职位、直属上级、启用状态 |
| AI校验规则 | 身份证号校验（18位+校验位）；手机号格式校验；部门编码存在性验证(引用A2)；员工编号唯一性 |

```
AI智能映射示例:
客户Excel列名             →  系统标准字段
"工号"/"编号"/"工牌号"     →  员工编号
"姓名"/"名字"/"员工姓名"   →  姓名
"部门"/"所在部门"/"科室"   →  所属部门
"岗位"/"职务"/"职称"       →  职位
"联系电话"/"手机"/"电话"   →  手机号
```

**A4. 用户档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | 基于员工档案自动生成 / 客户指定 |
| AI处理能力 | 根据岗位角色自动推荐系统权限组；自动生成用户账号（规则可配） |
| 字段清单 | 用户账号、用户名称、关联员工、所属角色/权限组、初始密码策略、启用状态 |
| AI校验规则 | 账号唯一性；关联员工存在性(引用A3)；至少一个管理员账号 |

```
AI自动化能力:
1. 根据部门+职位自动推荐角色分配:
   - 总经理/副总 → "高管查看"权限组
   - 销售经理    → "销售管理"权限组
   - 业务员      → "销售操作"权限组
   - 仓管员      → "仓库操作"权限组
   - 磅房人员    → "计量操作"权限组
   - 财务主管    → "财务管理"权限组
   - 出纳        → "资金操作"权限组
   - 会计        → "财务操作"权限组
2. 账号生成规则：姓名拼音 / 工号 / 自定义规则
3. 初始密码策略推荐
```

**A5. 账号资料（银行/现金账户）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户财务提供的银行开户信息 |
| AI处理能力 | 银行名称标准化；账号格式校验；自动关联对应财务科目 |
| 字段清单 | 账号编码、账号名称、账号类型(银行/现金/微信/支付宝/其他)、银行全称、银行简称、银行账号、开户行网点、币种、启用状态、对应科目编码 |
| AI校验规则 | 银行账号格式校验；银行名称标准化匹配；账号编码唯一性；至少一个现金账户 |

```
AI标准化示例:
"工行"/"工商银行"/"ICBC"/"中国工商"  → "中国工商银行"
"建行"/"建设银行"/"CCB"             → "中国建设银行"
自动匹配科目：银行账号 → "银行存款-XX银行" 科目
```

---

##### B类：业务档案类数据

**B1. 客户档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | Excel客户列表 / 旧系统导出 / 名片扫描 |
| AI处理能力 | 客商名称去重合并；地址标准化；信用评级建议；行业分类 |
| 字段清单 | 客户编码、客户全称、客户简称、客户类型(企业/个人)、统一社会信用代码、联系人、联系电话、传真、邮箱、地址(省/市/区/详细)、所属行业、客户分类(终端用户/贸易商/加工厂/工程项目)、结算方式(现款/月结/账期)、信用额度、税率、开票资料(名称/税号/地址电话/银行账号)、业务员(对应)、启用状态、备注 |
| AI校验规则 | 客户名称去重(相似度>85%标记)；统一社会信用代码格式校验；手机/电话格式；地址解析到省市区 |

```
AI去重合并示例:
以下识别为同一客户，建议合并：
  - "上海建工集团"
  - "上海建工集团股份有限公司"  
  - "上海建工"
  → 合并为："上海建工集团股份有限公司"（取最完整名称）
  → 其余作为简称保留

AI分类推荐:
根据客户名称中的关键词自动推荐分类：
  - 含"钢铁"/"钢材"/"金属材料" → 贸易商
  - 含"建设"/"建筑"/"工程"     → 工程项目
  - 含"制造"/"机械"/"汽车"     → 终端用户
  - 含"加工"/"剪切"/"开平"     → 加工厂
```

**B2. 供应商档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | Excel供应商列表 / 旧系统导出 |
| AI处理能力 | 供应商名称标准化与去重；钢厂/贸易商自动分类；供应商与已有客户交叉比对（同时为客户和供应商的情况） |
| 字段清单 | 供应商编码、供应商全称、供应商简称、供应商类型(钢厂/贸易商/加工厂/物流商/其他)、统一社会信用代码、联系人、联系电话、传真、邮箱、地址(省/市/区/详细)、结算方式、税率、开票资料(名称/税号/地址电话/银行账号)、采购员(对应)、启用状态、备注 |
| AI校验规则 | 供应商名称去重；与客户档案交叉比对（双重身份标记）；统一社会信用代码校验 |

```
AI特殊处理:
1. 钢厂名称标准化（内置全国主要钢厂名称库）:
   "宝钢"/"宝山钢铁"/"上海宝钢"/"宝武"
     → 标准全称："宝山钢铁股份有限公司"
     → 标准简称："宝钢"
     → 集团归属："中国宝武钢铁集团"
   
   "沙钢"/"江苏沙钢"
     → "江苏沙钢集团有限公司"
   
   "鞍钢"/"鞍山钢铁"
     → "鞍钢股份有限公司"

2. 自动识别供应商类型:
   - 匹配钢厂名称库 → 钢厂
   - 含"物流"/"运输"/"仓储" → 物流商
   - 含"加工"/"剪切" → 加工厂
   - 其他 → 贸易商

3. 客商交叉检查:
   自动比对客户档案与供应商档案，标记同时为客户和供应商的企业
   → 在系统中标记为"双重身份"，方便后续对账
```

**B3. 货品类别**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户经营品类说明 / 调研数据 / 旧系统导出 |
| AI处理能力 | 基于钢铁品类知识库自动构建分类树；根据客户经营范围推荐品类体系 |
| 字段清单 | 类别编码、类别名称、上级类别、层级、排序、计量单位(默认)、计价单位(默认)、启用状态 |
| AI校验规则 | 类别编码唯一性；树形完整性；叶子节点才能挂货品 |

```
AI内置钢铁品类知识树（推荐模板）:

├─ 板材
│  ├─ 热轧卷板(HRC)
│  ├─ 冷轧卷板(CRC)
│  ├─ 镀锌板(GI/GA)
│  ├─ 彩涂板
│  ├─ 中厚板
│  ├─ 花纹板
│  └─ 不锈钢板
├─ 型材
│  ├─ H型钢
│  ├─ 工字钢
│  ├─ 槽钢
│  ├─ 角钢
│  └─ 扁钢
├─ 管材
│  ├─ 无缝管
│  ├─ 焊管
│  ├─ 方矩管
│  └─ 镀锌管
├─ 线材
│  ├─ 高线
│  ├─ 普线
│  └─ 盘螺
├─ 建材
│  ├─ 螺纹钢
│  └─ 圆钢
└─ 其他
   ├─ 废钢
   └─ 原料

AI处理策略:
1. 根据客户调研结果中的经营品类，从知识树中裁剪出匹配的子集
2. 如果客户有自定义品类，AI评估是否可归入标准品类
3. 如果客户旧系统有品类数据，AI做映射匹配
```

**B4. 货品方案（产品/SKU档案）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户现有货品清单Excel / 旧系统导出 / 进销存记录 |
| AI处理能力 | 品名标准化、规格标准化、材质标准化、钢厂标准化；智能去重合并 |
| 字段清单 | 货品编码、品名、规格(厚×宽×长/C)、材质、钢厂/产地、表面/镀层、货品类别(引用B3)、计量单位(吨/件/张/米/卷)、计价单位、件重(理论/实际)、税率、启用状态、备注 |
| AI校验规则 | 品名+规格+材质+钢厂组合唯一性；规格格式合规性；材质代码合规性；类别引用有效性 |

```
这是AI数据治理的核心难点，需要钢铁行业深度知识：

1. 品名标准化（同义词库）:
   ┌──────────────────┬───────────────────────────┐
   │ 标准品名          │ 同义词/别名                 │
   ├──────────────────┼───────────────────────────┤
   │ 热轧卷板          │ 热轧卷、热卷、HRC、热板      │
   │ 冷轧卷板          │ 冷轧卷、冷卷、CRC、冷板、SPCC│
   │ 镀锌板            │ 镀锌卷、白铁皮、GI            │
   │ 热轧酸洗板        │ 酸洗板、酸洗卷、PL            │
   │ 螺纹钢            │ 螺纹、HRB400、抗震钢筋、螺纹钢筋│
   │ H型钢             │ H钢、H形钢、H型              │
   │ 无缝钢管          │ 无缝管、无缝                  │
   └──────────────────┴───────────────────────────┘

2. 规格标准化引擎:
   输入各种格式 → 解析 → 输出标准格式

   板材/卷板: 厚度*宽度*长度(或C)
     "3.0*1250*C"    → { 厚:3.0, 宽:1250, 长:"C(卷)" }  ✓ 标准
     "3.0×1250×C"    → 转换为 "3.0*1250*C"
     "3*1250*卷"     → 转换为 "3.0*1250*C"
     "3.0mm*1250mm*C"→ 转换为 "3.0*1250*C"

   型材: 规格型号
     "H200*200*8*12" → { H:200, B:200, tw:8, tf:12 }
     "HW200×200"     → 补全为 "H200*200*8*12"(查标准表)

   管材: 外径*壁厚*长度
     "Φ219*8*12000"  → { OD:219, WT:8, L:12000 }
     "219×8×12m"     → 转换为 "Φ219*8*12000"

   线材/建材: 直径
     "Φ8"/"φ8"/"8mm" → "Φ8"
   
3. 材质标准化引擎:
   "Q235"    → "Q235B"  (默认补全子级)
   "q235b"   → "Q235B"  (大写标准化)
   "Q345"    → "Q345B"  (默认补全子级)
   "SS400"   → "Q235B"  (日标转国标映射)
   "SPHC"    → 保留      (日标热轧通用，标记为进口)
   "DC01"    → 保留      (欧标，标记为进口)
   
   AI额外校验: 品名与材质是否匹配
   例: "螺纹钢" + "SPCC" → 告警（SPCC通常用于冷轧，不用于螺纹钢）

4. 钢厂名称标准化:
   使用B2中的钢厂名称库进行统一

5. 件重合理性校验(基于理论计算):
   板材理论重量 = 厚(mm) × 宽(mm) × 长(mm) × 密度(7.85g/cm³)
   偏差超过±5%标记为异常，提醒人工确认
```

**B5. 仓库档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户仓库信息表 / 调研数据 |
| AI处理能力 | 仓库名称标准化；根据仓库类型推荐管理配置 |
| 字段清单 | 仓库编码、仓库名称、仓库类型(自有/租赁/第三方)、仓库性质(钢材库/成品库/半成品库/废料库/虚拟库)、管理方式(按库位/不按库位)、地址、联系人、联系电话、面积、容量、启用状态 |
| AI校验规则 | 仓库编码唯一性；至少一个主仓库 |

```
AI推荐配置:
根据仓库类型自动推荐:
- 钢材库 → 建议按库位管理，启用库位货品绑定
- 加工件库 → 建议按批次管理
- 在途库(虚拟库) → 用于跟踪已发未到货物
- 寄售库(虚拟库) → 用于寄售业务
```

**B6. 库位档案**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户库位规划图 / 库位清单 |
| AI处理能力 | 从库位规划图中识别并提取库位信息；自动生成库位编码规则 |
| 字段清单 | 库位编码、库位名称、所属仓库(引用B5)、库位类型(货架/地面/料场)、可存品类、容量上限、启用状态 |
| AI校验规则 | 库位编码唯一性；所属仓库引用有效性；仓库管理方式为"按库位"时库位不能为空 |

---

##### C类：财务基础类数据

**C1. 财务科目（会计科目体系）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户现有会计科目表Excel / 财务软件导出 / 按行业模板生成 |
| AI处理能力 | 自动识别会计制度类型；科目体系完整性检查；辅助核算维度推荐 |
| 字段清单 | 科目编码、科目名称、上级科目、科目类别(资产/负债/权益/损益)、余额方向(借/贷)、辅助核算(客户/供应商/部门/员工/项目)、是否末级、启用状态 |
| AI校验规则 | 编码层级规范(如4-2-2-2)；一级科目对照国标；借贷方向与科目类别一致性；末级科目标记正确性 |

```
AI自动处理流程:
1. 如果客户有现成科目表 → 智能映射到系统标准结构
2. 如果客户没有科目表 → 基于行业模板自动生成

钢铁行业特色科目推荐:
一般企业标准科目 + 以下行业扩展:
├─ 1405 库存商品
│  ├─ 140501 板材
│  ├─ 140502 型材
│  ├─ 140503 管材
│  ├─ 140504 线材
│  └─ 140505 建材
├─ 5401 主营业务成本
│  ├─ 540101 板材成本
│  ├─ 540102 型材成本
│  └─ ...
└─ 辅助核算维度建议:
   - 应收账款 → 客户辅助核算
   - 应付账款 → 供应商辅助核算
   - 库存商品 → 仓库辅助核算
   - 销售费用 → 部门辅助核算
```

---

##### D类：期初余额类数据

**D1. 库存期初数**

| 维度 | 说明 |
|------|------|
| 数据来源 | 盘点表Excel / 旧系统库存快照 / ERP导出 |
| AI处理能力 | 货品信息标准化映射(复用B4规则)；重量/金额合理性校验；汇总与总账科目余额交叉核对 |
| 字段清单 | 货品编码(引用B4)、品名、规格、材质、钢厂、仓库(引用B5)、库位(引用B6)、件数、重量(吨)、单价(元/吨)、金额(元)、批次号/炉号、入库日期 |
| AI校验规则 | 货品编码存在性；仓库/库位引用有效性；金额=重量×单价(允差0.01)；单价合理性(对比市场价±30%)；汇总金额=科目余额"库存商品"科目(D5) |

```
AI高级校验:
1. 单价合理性检查:
   - 对比近期市场价格（可接入钢铁价格指数API）
   - 热轧卷板 Q235B 3.0*1250*C 当前市价约 3,800元/吨
   - 如果期初单价为 38,000元/吨 → 标红：可能是单位错误（元 vs 元/吨）
   - 如果期初单价为 380元/吨 → 标红：价格异常偏低

2. 件重合理性:
   - 每件重量 = 总重量 / 件数
   - 对比该规格的理论件重，偏差过大告警

3. 库存汇总 → 科目余额对账:
   Σ库存金额 必须等于 科目余额中"库存商品"科目的期初余额
   不平时自动列示差异明细
```

**D2. 客户欠款期初数（应收账款明细）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 客户应收明细表Excel / 旧系统导出 / 财务账簿 |
| AI处理能力 | 客户名称模糊匹配到客户档案(B1)；账龄自动计算；汇总与科目余额交叉核对 |
| 字段清单 | 客户编码(引用B1)、客户名称、单据类型(销售单/其他)、单据编号、单据日期、应收金额、已收金额、欠款余额、预计回款日期、备注 |
| AI校验规则 | 客户编码存在性；欠款余额=应收金额-已收金额；Σ客户欠款=科目余额"应收账款"(D5)；账龄超365天标记预警 |

```
AI智能匹配:
客户名称模糊匹配到已导入的客户档案:
  "上海建工" → 匹配到 B1 中 "上海建工集团股份有限公司" (置信度:92%)
  "张老板"   → 无法匹配 → 标记为待人工确认

AI账龄分析:
自动按单据日期计算账龄并分档:
  ├─ 0-30天     xxx元  (正常)
  ├─ 31-90天    xxx元  (关注)
  ├─ 91-180天   xxx元  (预警)  ← 标黄
  ├─ 181-365天  xxx元  (风险)  ← 标橙
  └─ 365天以上  xxx元  (高风险) ← 标红，建议计提坏账
```

**D3. 供应商期初数（应付账款明细）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 供应商应付明细表Excel / 旧系统导出 |
| AI处理能力 | 供应商名称匹配到供应商档案(B2)；账龄分析；汇总核对 |
| 字段清单 | 供应商编码(引用B2)、供应商名称、单据类型(采购单/其他)、单据编号、单据日期、应付金额、已付金额、欠款余额、付款期限、备注 |
| AI校验规则 | 供应商编码存在性；欠款余额=应付金额-已付金额；Σ供应商期初=科目余额"应付账款"(D5) |

**D4. 账户余额（现金/银行账户期初余额）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 银行对账单 / 财务提供的余额确认书 |
| AI处理能力 | 账户名称匹配到账号资料(A5)；余额合理性校验 |
| 字段清单 | 账号编码(引用A5)、账号名称、币种、期初余额、备注 |
| AI校验规则 | 账号引用有效性；Σ银行余额=科目余额"银行存款"(D5)；现金余额=科目余额"库存现金"(D5)；余额不能为负 |

**D5. 科目余额（总账科目期初余额）**

| 维度 | 说明 |
|------|------|
| 数据来源 | 财务软件导出的科目余额表 / 手工试算平衡表 |
| AI处理能力 | 科目编码映射；试算平衡校验；与明细数据交叉核对 |
| 字段清单 | 科目编码(引用C1)、科目名称、期初余额(借方)、期初余额(贷方)、辅助项目(客户/供应商/部门，如有) |
| AI校验规则 | 科目编码存在性；**试算平衡：Σ借方余额 = Σ贷方余额**；末级科目才能录入余额 |

```
AI核心校验 — 试算平衡与交叉核对:

  ┌────────────────────────────────────────────────────────────┐
  │                    试算平衡校验                              │
  │                                                              │
  │  Σ所有科目借方余额 = Σ所有科目贷方余额                        │
  │  （差异为0才允许通过）                                       │
  └────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────────┐
  │                   交叉核对关系                               │
  │                                                              │
  │  科目余额"库存商品"     ═══  Σ库存期初数(D1).金额            │
  │  科目余额"应收账款"     ═══  Σ客户欠款期初数(D2).欠款余额     │
  │  科目余额"应付账款"     ═══  Σ供应商期初数(D3).欠款余额       │
  │  科目余额"银行存款"     ═══  Σ账户余额(D4).银行账户余额       │
  │  科目余额"库存现金"     ═══  Σ账户余额(D4).现金账户余额       │
  │  科目余额"其他应收款"   ═══  Σ其他应收余额(D6)               │
  │  科目余额"其他应付款"   ═══  Σ其他应付余额(D7)               │
  │                                                              │
  │  以上任一不等 → 标记差异金额，列示明细，辅助排查               │
  └────────────────────────────────────────────────────────────┘
```

**D6. 其他应收余额**

| 维度 | 说明 |
|------|------|
| 数据来源 | 财务明细表 |
| AI处理能力 | 对象名称匹配(客户/员工/其他)；账龄分析 |
| 字段清单 | 对象类型(客户/员工/个人/单位)、对象编码、对象名称、单据摘要、金额、发生日期、备注 |
| AI校验规则 | 对象存在性(客户引用B1/员工引用A3)；Σ金额=科目余额"其他应收款"(D5) |

**D7. 其他应付余额**

| 维度 | 说明 |
|------|------|
| 数据来源 | 财务明细表 |
| AI处理能力 | 对象名称匹配；到期日预警 |
| 字段清单 | 对象类型(供应商/员工/个人/单位)、对象编码、对象名称、单据摘要、金额、发生日期、备注 |
| AI校验规则 | 对象存在性(供应商引用B2/员工引用A3)；Σ金额=科目余额"其他应付款"(D5) |

---

##### E类：税务发票类数据

**E1. 应开销项发票期初数**

| 维度 | 说明 |
|------|------|
| 数据来源 | 已销售但尚未开票的明细表 / 财务统计 |
| AI处理能力 | 客户+货品信息交叉引用校验；税额自动计算校验 |
| 字段清单 | 客户编码(引用B1)、客户名称、销售单号、货品编码(引用B4)、品名、规格、材质、数量、单价、金额(不含税)、税率、税额、价税合计、销售日期、备注 |
| AI校验规则 | 客户/货品引用有效性；税额=金额×税率(允差0.01)；价税合计=金额+税额；税率合规性(13%/9%/6%/0%) |

```
AI处理说明:
应开销项发票 = 已经发生销售行为、确认了收入，但尚未给客户开具增值税发票的部分
这部分数据确保新旧系统切换时，开票状态不丢失

AI自动校验:
1. 每一行的客户必须在客户档案(B1)中存在
2. 每一行的货品必须在货品方案(B4)中存在
3. 税额计算: 金额 × 税率，允许尾差±0.01元
4. 汇总统计: 按客户汇总应开票金额，供PM确认
```

**E2. 应收进项发票期初数**

| 维度 | 说明 |
|------|------|
| 数据来源 | 已采购但尚未收到供应商发票的明细表 / 财务统计 |
| AI处理能力 | 供应商+货品信息交叉引用校验；税额自动计算校验 |
| 字段清单 | 供应商编码(引用B2)、供应商名称、采购单号、货品编码(引用B4)、品名、规格、材质、数量、单价、金额(不含税)、税率、税额、价税合计、采购日期、备注 |
| AI校验规则 | 供应商/货品引用有效性；税额=金额×税率(允差0.01)；价税合计=金额+税额；税率合规性 |

```
AI处理说明:
应收进项发票 = 已经发生采购行为、货物已入库，但尚未从供应商处收到增值税发票的部分

AI汇总报告:
生成《发票期初数据汇总》:
  ┌──────────────┬──────────┬──────────┐
  │ 汇总维度      │ 应开销项  │ 应收进项  │
  ├──────────────┼──────────┼──────────┤
  │ 涉及客商数    │ xx家     │ xx家      │
  │ 不含税金额    │ xxx元    │ xxx元     │
  │ 税额合计      │ xxx元    │ xxx元     │
  │ 价税合计      │ xxx元    │ xxx元     │
  │ 最早日期      │ xxxx-xx  │ xxxx-xx   │
  │ 最晚日期      │ xxxx-xx  │ xxxx-xx   │
  └──────────────┴──────────┴──────────┘
```

---

#### 4.6.4 总体工作流设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                    期初数据处理总体工作流                               │
│                                                                       │
│  ┌─────────┐                                                        │
│  │ Step 1   │  数据收集 & 模板下发                                    │
│  │          │  AI根据项目调研结果，生成定制化的数据收集模板(Excel)        │
│  │          │  按5大类21项分别生成，每个模板含填写说明和示例             │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 2   │  数据上传 & 格式识别                                    │
│  │          │  支持: Excel(.xlsx/.xls) / CSV / 旧系统DB导出 / 图片OCR │
│  │          │  AI自动识别数据类型，匹配到21项中的对应项                  │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 3   │  智能字段映射                                          │
│  │          │  AI分析列名/数据特征，自动映射到标准字段                   │
│  │          │  未匹配字段 → 人工指定                                   │
│  │          │  映射规则可保存复用                                      │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 4   │  数据清洗 & 标准化                                     │
│  │          │  执行各数据项专属的AI清洗规则                             │
│  │          │  输出: 清洗报告（修改了什么、为什么修改）                  │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 5   │  规则校验 & 交叉核对                                   │
│  │          │  单项校验: 格式、范围、引用完整性                         │
│  │          │  跨项校验: 余额勾稽、引用一致性                          │
│  │          │  输出: 校验报告（通过/警告/错误 三级标记）                │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 6   │  人工确认 & 修正                                       │
│  │          │  PM在可视化界面审核AI处理结果                             │
│  │          │  错误项: 必须修正才能继续                                 │
│  │          │  警告项: 可确认忽略或修正                                 │
│  │          │  客户确认签字（电子签）                                   │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 7   │  按序导入 & 验证                                       │
│  │          │  按第4.6.2节定义的5个批次顺序执行导入                     │
│  │          │  每批次完成后执行验证，通过后启动下一批次                  │
│  │          │  失败自动回滚当前批次                                     │
│  └────┬────┘                                                        │
│       ▼                                                              │
│  ┌─────────┐                                                        │
│  │ Step 8   │  导入完成 & 最终报告                                    │
│  │          │  生成《期初数据导入报告》                                 │
│  │          │  包含: 各项数据量统计、校验结果、异常处理记录              │
│  └─────────┘                                                        │
└─────────────────────────────────────────────────────────────────────┘
```

---

#### 4.6.5 数据清洗规则总览（钢铁行业特化）

```
钢铁行业数据清洗规则集（汇总）:

1. 品名标准化
   - "热轧卷板" / "热轧卷" / "热卷" / "HRC" → 标准名称："热轧卷板"
   - 建立钢铁品类同义词库（约200+条映射规则）

2. 规格标准化
   - "3.0*1250*C" / "3.0×1250×C" / "3*1250*卷" → "3.0*1250*C"
   - 按品类区分规格格式（板材/型材/管材/线材各有标准格式）

3. 材质标准化
   - "Q235" / "Q235B" / "q235b" → "Q235B"
   - 国标材质代码校验 + 日标/欧标/美标转换提示

4. 钢厂名称标准化
   - "宝钢" / "宝山钢铁" / "上海宝钢" → "宝山钢铁股份有限公司"
   - 内置全国200+主要钢厂名称库

5. 客商名称去重与合并
   - 基于名称相似度 + 统一社会信用代码判重
   - 同一客商在客户和供应商中的交叉识别

6. 重量/金额单位统一
   - 重量: 吨/千克/公斤/磅 → 统一吨
   - 金额: 元/万元/千元 → 统一元

7. 地址标准化
   - 解析到省/市/区三级 + 详细地址
   - 基于地址库校验行政区划

8. 电话号码标准化
   - 手机号: 11位数字格式校验
   - 座机: 区号-号码 格式标准化

9. 日期格式统一
   - 支持识别: yyyy-MM-dd / yyyy/MM/dd / yyyyMMdd / yyyy年M月d日
   - 统一输出: yyyy-MM-dd

10. 勾稽关系交叉校验
    - 库存金额汇总 = "库存商品"科目余额
    - 客户欠款汇总 = "应收账款"科目余额
    - 供应商欠款汇总 = "应付账款"科目余额
    - 银行/现金余额 = 对应科目余额
    - 其他应收/应付 = 对应科目余额
    - 科目余额试算平衡（借=贷）
```

---

#### 4.6.6 帐套配置自动化

**能力说明**：根据客户画像自动推荐和配置帐套参数

```
客户画像 → 帐套模板匹配 → 参数配置生成 → 人工确认 → 自动创建

配置项包括：
- 科目体系（根据客户会计制度，结合C1财务科目数据）
- 仓库/库位结构（根据B5/B6仓库库位档案自动生成）
- 品类/规格体系（根据B3/B4货品类别和方案自动生成）
- 计价方式（移动加权/先进先出/个别计价）
- 审批流程（根据A2部门架构+客户组织需求配置）
- 权限矩阵（根据A4用户档案+岗位职责自动分配）
- 单据模板（根据客户业务流程定制）
- 编码规则（各档案的编码生成规则配置）
```

---

#### 4.6.7 AI数据收集模板自动生成

**核心能力**：根据项目调研结果，为客户定制生成21项数据收集Excel模板

```
每个模板包含:
┌─────────────────────────────────────────────┐
│ Sheet 1: 填写说明                             │
│  - 本表用途说明                               │
│  - 各列字段说明                               │
│  - 填写注意事项                               │
│  - 常见问题解答                               │
│                                               │
│ Sheet 2: 数据填写区                            │
│  - 标准列头（带数据验证）                       │
│  - 示例数据行（根据客户行业定制示例）            │
│  - 下拉选项（如：仓库名称、货品类别、部门名称）  │
│                                               │
│ Sheet 3: 参考数据                              │
│  - 可选项字典                                 │
│  - 编码规则说明                               │
└─────────────────────────────────────────────┘

AI定制逻辑:
- 根据客户经营品类，在货品方案模板的示例中预填匹配的品类示例
- 根据客户已知的仓库信息，在仓库档案模板中预填
- 根据客户规模，调整模板的复杂度（小客户简化、大客户细化）
- 根据纳税人类型(一般/小规模)，调整税率选项
```

---

### 4.7 上线检查智能体（Go-Live Check Agent）

**对应环节**：⑨ 启用帐套

**核心能力**：
- 自动执行上线前检查清单
- 数据完整性最终验证
- 系统配置合规性检查
- 生成《上线准备度评估报告》

**检查项自动化**：

```
上线检查清单:

┌─ 基础设置检查 (A类)
│  ├─ A1 企业资料完整性（统一社会信用代码、开票信息）
│  ├─ A2 部门架构完整性（无孤立节点、树形合理）
│  ├─ A3 员工档案完整性（关键岗位均已录入）
│  ├─ A4 用户账号 & 权限分配（每个岗位至少一个账号）
│  └─ A5 银行/现金账号资料（至少一个现金户+一个银行户）
│
├─ 业务档案检查 (B类)
│  ├─ B1 客户档案（有活跃业务的客户全部录入）
│  ├─ B2 供应商档案（有活跃业务的供应商全部录入）
│  ├─ B3 货品类别（树形结构完整，覆盖全部经营品类）
│  ├─ B4 货品方案（覆盖期初库存涉及的全部货品）
│  ├─ B5 仓库档案（全部实体仓+必要虚拟仓）
│  └─ B6 库位档案（启用库位管理的仓库，库位已配齐）
│
├─ 财务基础检查 (C类)
│  └─ C1 财务科目（科目体系完整，辅助核算维度已配置）
│
├─ 期初余额检查 (D类) ★关键
│  ├─ D1 库存期初数 — 货品/仓库引用有效、金额=重量×单价
│  ├─ D2 客户欠款 — 客户引用有效、明细合计=科目余额
│  ├─ D3 供应商欠款 — 供应商引用有效、明细合计=科目余额
│  ├─ D4 账户余额 — 账号引用有效、合计=科目余额
│  ├─ D5 科目余额 — ★试算平衡（Σ借方 = Σ贷方）
│  ├─ D6 其他应收 — 对象引用有效、合计=科目余额
│  ├─ D7 其他应付 — 对象引用有效、合计=科目余额
│  └─ 全局勾稽: D1+D2+D3+D4+D6+D7 各明细汇总与D5科目余额一致
│
├─ 税务发票检查 (E类)
│  ├─ E1 应开销项发票 — 客户/货品引用有效、税额计算正确
│  └─ E2 应收进项发票 — 供应商/货品引用有效、税额计算正确
│
├─ 系统配置检查
│  ├─ 审批流程配置正确性
│  ├─ 权限矩阵完整性
│  ├─ 打印模板配置
│  ├─ 编码规则配置
│  └─ 接口配置（如有）
│
├─ 功能验证检查
│  ├─ 核心业务流程走通测试（采购→入库→销售→出库→结算→开票）
│  ├─ 报表数据准确性（库存报表/应收报表/科目余额表 与期初数一致）
│  └─ 预警规则配置
│
└─ 人员准备检查
   ├─ 关键用户培训完成确认（A4中的关键角色账号对应的人员）
   ├─ 账号权限分配确认
   └─ 应急联络人确认
```

---

### 4.8 现场辅助智能体（On-Site Assistant Agent）

**对应环节**：⑩ 现场跟进辅助

**核心能力**：
- 替代部分驻场人员职能，通过AI客服7×24小时远程支持
- 智能问题分级：能解决的立即解决，不能解决的精准升级
- 操作异常监控与主动预警

**分级响应机制**：

```
┌─────────────────────────────────────────────┐
│              问题分级响应矩阵                  │
├──────┬──────────────┬───────────────────────┤
│ 级别  │ 问题类型      │ 处理方式               │
├──────┼──────────────┼───────────────────────┤
│ L1   │ 操作类问题    │ AI直接回答             │
│      │ (怎么操作xxx) │ (知识库+RAG)           │
├──────┼──────────────┼───────────────────────┤
│ L2   │ 配置类问题    │ AI给出方案，人工确认执行 │
│      │ (需调整设置)  │                        │
├──────┼──────────────┼───────────────────────┤
│ L3   │ 流程类问题    │ AI初步分析，转PM处理    │
│      │ (业务逻辑疑问)│                        │
├──────┼──────────────┼───────────────────────┤
│ L4   │ 系统Bug      │ AI收集信息，转研发处理   │
│      │ (功能异常)    │                        │
└──────┴──────────────┴───────────────────────┘
```

**主动预警能力**：
- 监控用户操作日志，识别异常操作模式（如：反复撤销、频繁报错）
- 监控数据异常（如：库存出现负数、价格异常偏离）
- 主动推送操作建议和风险提醒

---

### 4.9 交付报告智能体（Delivery Report Agent）

**对应环节**：⑪ 成果交付确认

**核心能力**：
- 自动采集项目全过程数据
- 生成《项目交付成果报告》
- 交付成果与项目目标的达成度对比分析

**报告结构**：

```
《项目交付成果报告》

1. 项目概况
   - 基本信息（客户、周期、团队、范围）
   - 里程碑达成情况

2. 交付成果总结
   - 系统上线模块清单
   - 数据迁移完成情况
   - 接口对接完成情况
   - 定制开发完成情况

3. 目标达成分析
   ┌──────────┬──────────┬──────────┬──────┐
   │ 目标类别  │ 目标描述  │ 达成状态  │ 说明 │
   ├──────────┼──────────┼──────────┼──────┤
   │ 管理目标  │ xxx     │ ✓已达成   │ ...  │
   │ 流程目标  │ xxx     │ ✓已达成   │ ...  │
   │ 效率目标  │ xxx     │ △部分达成 │ ...  │
   │ 风控目标  │ xxx     │ ✓已达成   │ ...  │
   └──────────┴──────────┴──────────┴──────┘

4. 培训完成情况
   - 培训场次/人次统计
   - 考核通过率

5. 项目过程质量
   - 进度偏差分析
   - 问题处理统计
   - 客户满意度

6. 遗留问题与建议
7. 后续维护计划
```

---

### 4.10 售后服务智能体（After-Sales Agent）

**对应环节**：⑫ 售后跟进

**核心能力**：
- 7×24小时智能客服，处理常见问题
- 智能工单系统：问题分类、优先级判定、自动派单
- 客户健康度监控与主动关怀

**系统设计**：

#### 4.10.1 智能客服

```
多渠道接入（微信/网页/电话语音转文字）
         │
         ▼
    意图识别引擎
    ├─ 操作咨询 → RAG知识库检索 → 直接回答
    ├─ 故障报修 → 自动创建工单 → 智能派单
    ├─ 需求建议 → 记录 → 定期汇总反馈
    └─ 投诉处理 → 升级 → 通知客户成功经理
```

#### 4.10.2 智能工单流转

```
工单创建（AI自动或人工）
    │
    ▼
AI分类 & 优先级判定
    │
    ├─ P0 紧急（系统不可用）→ 立即通知值班工程师
    ├─ P1 高（核心功能异常）→ 4小时内响应
    ├─ P2 中（非核心问题）→ 1个工作日内响应
    └─ P3 低（优化建议）→ 排期处理
    │
    ▼
智能派单（技能匹配 + 负载均衡）
    │
    ▼
处理过程跟踪 → AI辅助解决方案推荐
    │
    ▼
解决确认 → 知识沉淀 → 知识库更新
```

#### 4.10.3 客户健康度监控

**监控指标**：
- 系统使用活跃度（DAU/MAU、功能使用深度）
- 问题工单趋势（频率、类型分布）
- 关键用户流失预警
- 续费/增购意向信号

**主动关怀触发规则**：
- 使用活跃度连续下降 → 触发回访
- 高频重复问题 → 推送专项培训
- 新功能上线 → 推送更新指南
- 合同到期前 → 触发续约流程

---

## 五、智能体间协作流程

### 5.1 全流程智能体协作编排

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        项目交付全流程编排                                 │
│                                                                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────────┐      │
│  │①项目调度  │───▶│②调研分析  │───▶│③计划生成  │───▶│④调研分析(深度)│      │
│  │  Agent   │    │  Agent   │    │  Agent   │    │   Agent      │      │
│  └──────────┘    └──────────┘    └──────────┘    └──────────────┘      │
│       │               │               │                │               │
│       │         客户画像(初)      交付计划         客户画像(完整)          │
│       │               │               │           背景分析报告           │
│       ▼               ▼               ▼                │               │
│  ┌──────────────────────────────────────────────────────┘               │
│  │                                                                      │
│  ▼                                                                      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │⑤演练配置  │───▶│⑥培训助手  │───▶│⑦数据治理  │───▶│⑧帐套管理  │         │
│  │  Agent   │    │  Agent   │    │  Agent   │    │  Agent   │         │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘         │
│       │               │               │                │               │
│   演练环境/脚本    培训材料/考核      清洗后的数据      帐套配置完成        │
│       │               │               │                │               │
│       ▼               ▼               ▼                ▼               │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │⑨上线检查  │───▶│⑩现场辅助  │───▶│⑪交付报告  │───▶│⑫售后服务  │         │
│  │  Agent   │    │  Agent   │    │  Agent   │    │  Agent   │         │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘         │
│       │               │               │                │               │
│   准备度报告       问题处理记录      交付成果报告     持续运营支持         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 上下文传递机制

各智能体之间通过**项目上下文（Project Context）**共享信息：

```
ProjectContext {
  project_id: "PRJ-2026-001",
  customer_profile: { ... },       // 客户画像，由调研Agent维护
  delivery_plan: { ... },          // 交付计划，由计划Agent维护
  training_records: { ... },       // 培训记录，由培训Agent维护
  data_migration_status: { ... },  // 数据迁移状态，由数据Agent维护
  issue_logs: [ ... ],             // 问题记录，由辅助/售后Agent维护
  milestone_status: { ... },       // 里程碑状态，由编排引擎维护
  documents: [ ... ],              // 项目文档索引
  ai_decisions_log: [ ... ]        // AI决策审计日志
}
```

### 5.3 人工审核节点

以下关键节点必须设置人工审核确认：

| 节点 | 审核人 | 审核内容 |
|------|--------|---------|
| PM分配确认 | 部门总监 | 确认AI推荐的PM人选 |
| 调研报告确认 | PM | 确认AI生成的调研报告准确性 |
| 交付计划确认 | PM + 总监 | 确认交付计划的合理性 |
| 数据导入确认 | PM + 客户 | 确认数据清洗结果和导入数据 |
| 上线确认 | PM + 客户 | 确认上线检查通过 |
| 交付验收确认 | 客户 | 确认交付成果 |

---

## 六、技术架构设计（Java + Vue3）

### 6.1 系统总体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        前端层 (Vue3)                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 项目管理  │ │ 调研工作台│ │ 培训中心  │ │ 售后工作台│           │
│  │ Dashboard│ │ Workspace│ │  Portal  │ │ Workspace│           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                        │
│  │ 数据治理  │ │ AI对话窗  │ │ 报告中心  │                        │
│  │ Workbench│ │ ChatPanel│ │  Center  │                        │
│  └──────────┘ └──────────┘ └──────────┘                        │
└──────────────────────────────────────────────────────────────────┘
                              │
                         API Gateway
                              │
┌──────────────────────────────────────────────────────────────────┐
│                     后端服务层 (Java/Spring Boot)                  │
│                                                                    │
│  ┌──────────────────────┐  ┌──────────────────────┐              │
│  │  业务服务集群          │  │  AI智能体服务集群       │              │
│  │  ├─ 项目管理服务       │  │  ├─ Agent编排服务      │              │
│  │  ├─ 客户管理服务       │  │  ├─ Agent运行时        │              │
│  │  ├─ 文档管理服务       │  │  ├─ RAG检索服务        │              │
│  │  ├─ 工单管理服务       │  │  ├─ 知识库管理服务      │              │
│  │  └─ 数据导入服务       │  │  ├─ Prompt管理服务     │              │
│  │                       │  │  └─ LLM网关服务        │              │
│  └──────────────────────┘  └──────────────────────┘              │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                      基础设施层                                │ │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐   │ │
│  │  │MySQL   │ │Redis   │ │ES      │ │MinIO   │ │Milvus/ │   │ │
│  │  │(业务库) │ │(缓存)   │ │(搜索)   │ │(文件)   │ │PgVector│   │ │
│  │  │        │ │        │ │        │ │        │ │(向量库) │   │ │
│  │  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘   │ │
│  └──────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                              │
┌──────────────────────────────────────────────────────────────────┐
│                     外部服务层                                     │
│  ┌────────┐ ┌────────┐ ┌────────────┐ ┌────────┐               │
│  │ LLM API│ │ 语音ASR │ │ OCR识别服务 │ │ 邮件/IM │               │
│  │(多模型) │ │  服务   │ │            │ │  通知   │               │
│  └────────┘ └────────┘ └────────────┘ └────────┘               │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 后端技术栈选型

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 基础框架 | Spring Boot 3.x | 主框架 |
| AI框架 | Spring AI / LangChain4j | LLM编排与调用 |
| 向量数据库 | Milvus 或 PgVector | RAG向量存储与检索 |
| 工作流引擎 | Flowable / Camunda | 智能体编排与业务流程 |
| 文档解析 | Apache POI + PDF解析 | Excel/Word/PDF处理 |
| 搜索引擎 | Elasticsearch | 知识库全文检索 |
| 文件存储 | MinIO | 文档/附件存储 |
| 消息队列 | RabbitMQ / RocketMQ | 异步任务处理 |
| 缓存 | Redis | 会话管理/缓存 |
| 数据库 | MySQL 8.x | 业务数据存储 |

### 6.3 前端技术栈选型

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 基础框架 | Vue 3 + TypeScript | 主框架 |
| UI组件库 | Element Plus / Ant Design Vue | 组件库 |
| 状态管理 | Pinia | 全局状态 |
| 图表 | ECharts | 数据可视化 |
| 甘特图 | dhtmlxGantt / vue-gantt | 项目计划 |
| 流程图 | AntV X6 / vue-flow | 业务流程可视化 |
| AI对话 | 自定义ChatPanel | 流式对话界面 |
| Markdown | md-editor-v3 | 报告预览/编辑 |
| 富文本 | WangEditor | 文档编辑 |

### 6.4 AI智能体运行时核心设计

```java
// 智能体基础接口设计思路
public interface Agent {
    String getId();
    String getName();
    AgentType getType();
    AgentCapability[] getCapabilities();
    
    // 执行入口
    AgentResult execute(AgentContext context, AgentRequest request);
    
    // 流式执行（用于对话场景）
    Flux<AgentEvent> executeStream(AgentContext context, AgentRequest request);
}

// 智能体上下文
public class AgentContext {
    private String projectId;
    private String userId;
    private ProjectContext projectContext;  // 项目全局上下文
    private ConversationHistory history;    // 对话历史
    private Map<String, Object> variables;  // 运行时变量
}

// 智能体编排引擎
public interface AgentOrchestrator {
    // 执行编排好的工作流
    WorkflowResult executeWorkflow(String workflowId, ProjectContext context);
    
    // 根据用户意图自动选择并调用合适的智能体
    AgentResult dispatch(String userIntent, AgentContext context);
}
```

### 6.5 RAG知识库架构

```
知识库分层结构:

┌─ 通用知识层
│  ├─ 产品功能文档
│  ├─ 操作手册
│  ├─ FAQ库
│  └─ 版本更新说明
│
├─ 行业知识层
│  ├─ 钢铁行业术语表
│  ├─ 钢铁行业业务流程模板
│  ├─ 钢铁品类/材质/规格知识
│  ├─ 行业法规政策
│  └─ 行业最佳实践
│
├─ 项目经验层
│  ├─ 历史项目案例
│  ├─ 典型问题解决方案
│  ├─ 交付经验总结
│  └─ 客户反馈汇总
│
└─ 项目私有层
   ├─ 特定客户的调研数据
   ├─ 特定项目的交付文档
   └─ 特定客户的QA记录
```

**向量化与检索策略**：
- 文档分块：按语义段落分块，保留上下文（chunk_size=512, overlap=64）
- 多路召回：关键词检索(BM25) + 语义检索(向量) + 知识图谱检索
- 重排序：使用Reranker模型对召回结果精排
- 权限隔离：项目私有层数据按项目ID隔离

---

## 七、数据体系设计

### 7.1 核心数据模型

```
┌────────────────┐     ┌────────────────┐     ┌────────────────┐
│    Project      │────▶│ CustomerProfile│     │  ProjectPlan   │
│    项目主表     │     │   客户画像      │     │   交付计划      │
├────────────────┤     ├────────────────┤     ├────────────────┤
│ project_id     │     │ customer_id    │     │ plan_id        │
│ customer_id    │     │ company_name   │     │ project_id     │
│ pm_id          │     │ business_model │     │ milestones[]   │
│ status         │     │ categories[]   │     │ wbs_items[]    │
│ start_date     │     │ inventory_info │     │ resources[]    │
│ end_date       │     │ sales_info     │     │ risks[]        │
│ current_phase  │     │ organization   │     │ gantt_data     │
└────────────────┘     │ goals          │     └────────────────┘
                       └────────────────┘
                       
┌────────────────┐     ┌────────────────┐     ┌────────────────┐
│ TrainingRecord  │     │ DataMigration  │     │  ServiceTicket │
│   培训记录      │     │  数据迁移      │     │   服务工单      │
├────────────────┤     ├────────────────┤     ├────────────────┤
│ record_id      │     │ migration_id   │     │ ticket_id      │
│ project_id     │     │ project_id     │     │ project_id     │
│ trainee_id     │     │ source_type    │     │ type           │
│ role           │     │ status         │     │ priority       │
│ course_id      │     │ total_records  │     │ status         │
│ score          │     │ clean_records  │     │ assigned_to    │
│ status         │     │ error_records  │     │ resolved_by_ai │
└────────────────┘     │ mapping_rules  │     │ solution       │
                       └────────────────┘     └────────────────┘

┌────────────────┐     ┌────────────────┐
│ KnowledgeBase   │     │ AgentDecisionLog│
│   知识库条目    │     │  AI决策日志     │
├────────────────┤     ├────────────────┤
│ kb_id          │     │ log_id         │
│ category       │     │ agent_id       │
│ title          │     │ project_id     │
│ content        │     │ input          │
│ vector_id      │     │ output         │
│ metadata       │     │ model_used     │
│ source         │     │ confidence     │
│ access_level   │     │ human_review   │
└────────────────┘     │ feedback       │
                       └────────────────┘
```

### 7.2 AI决策可追溯设计

每一次AI决策都需记录完整上下文，确保可审计：

```
AgentDecisionLog:
  - 触发时间
  - 触发者（系统自动/用户操作）
  - 使用的智能体
  - 输入数据摘要
  - 调用的LLM模型及版本
  - Prompt内容（脱敏后）
  - RAG检索到的知识片段
  - AI输出结果
  - 置信度评分
  - 是否经过人工审核
  - 人工审核结果（通过/修改/拒绝）
  - 人工修改内容（如有）
```

---

## 八、实施路径建议

### 8.1 分阶段实施计划

#### 第一阶段：基础设施 + P0智能体

**目标**：搭建AI智能体基础平台，上线最高价值的5个智能体

**范围**：
1. AI智能体基础平台搭建
   - LLM网关服务（支持多模型切换）
   - RAG检索服务 + 向量数据库
   - 知识库管理后台
   - Agent运行时引擎
   - AI对话前端组件

2. P0智能体开发
   - **计划生成Agent**（③）— 入手难度最低，见效快
   - **培训助手Agent**（⑥）— 降本效果明显
   - **数据治理Agent**（⑦）— 技术成熟，价值高
   - **现场辅助Agent**（⑩）— 降低驻场成本
   - **售后服务Agent**（⑫）— 独立性强，易落地

**技术关键项**：
- 钢铁行业知识库构建（术语、品类、业务流程）
- 历史项目数据整理与向量化
- 产品操作文档结构化

#### 第二阶段：P1智能体 + 流程串联

**目标**：补齐核心智能体，实现全流程AI协同

**范围**：
1. P1智能体开发
   - **调研分析Agent**（②④）
   - **帐套管理Agent**（⑧）
   - **交付报告Agent**（⑪）

2. 智能体编排引擎
   - 实现智能体间的上下文传递
   - 实现全流程自动编排
   - 人工审核节点集成

3. 知识库持续丰富
   - 项目经验自动沉淀
   - 问题解决方案自动归档

#### 第三阶段：P2智能体 + 智能化升级

**目标**：全面智能化，持续优化

**范围**：
1. P2智能体开发
   - **项目调度Agent**（①）
   - **演练配置Agent**（⑤）
   - **上线检查Agent**（⑨）

2. 智能化升级
   - 多模态能力（语音输入、图片识别）
   - 预测性分析（项目风险预测、客户流失预测）
   - 智能体自我学习与优化

### 8.2 各阶段价值预期

| 阶段 | 人力节省预估 | 效率提升预估 | 关键价值 |
|------|------------|------------|---------|
| 第一阶段 | 20-30% | 30-40% | 培训/售后/数据处理人力大幅减少 |
| 第二阶段 | 40-50% | 50-60% | 全流程提效，经验标准化 |
| 第三阶段 | 50-60% | 60-80% | 预测性运维，主动服务 |

---

## 九、风险与应对策略

### 9.1 技术风险

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| LLM输出不准确/幻觉 | 生成错误方案影响交付 | 关键环节设人工审核；使用RAG约束回答范围；置信度评分低于阈值自动标记 |
| 数据安全泄露 | 客户敏感数据外泄 | 数据脱敏处理；使用私有化部署LLM（如本地部署开源模型）；数据按项目隔离 |
| LLM API不稳定 | 服务中断 | 多模型备份（主备切换）；关键功能降级方案（退回人工模式）|
| 向量检索精度不够 | AI回答质量差 | 多路召回+重排序；持续优化分块策略；人工标注优化 |

### 9.2 业务风险

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 用户排斥AI | 推广困难 | 先从内部PM试用，再推广到客户；AI作为辅助工具而非替代 |
| 知识库冷启动 | 初期AI表现差 | 组织老PM进行知识贡献；历史项目资料批量导入 |
| 过度依赖AI | 团队能力退化 | 保持关键环节人工参与；定期进行无AI演练 |
| 行业特殊性 | 通用AI不适用 | 持续投入行业知识库建设；行业专家参与Prompt调优 |

### 9.3 LLM选型建议

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 文档生成/报告撰写 | 通义千问/文心一言/GPT-4 | 中文表达能力强 |
| 数据分析/代码生成 | DeepSeek / GPT-4 | 逻辑推理能力强 |
| 知识问答/客服 | 私有化部署 Qwen/GLM + RAG | 数据安全，成本可控 |
| 数据清洗/格式化 | 轻量级模型(Qwen-7B) | 任务明确，无需大模型 |

> **成本控制建议**：对于高频低复杂度场景（如售后问答），优先使用私有化部署的开源模型降低Token成本；对于低频高复杂度场景（如计划生成），可使用商业API获得更好效果。

---

## 附录：术语表

| 术语 | 说明 |
|------|------|
| Agent/智能体 | 具有特定能力的AI自主执行单元 |
| RAG | 检索增强生成（Retrieval-Augmented Generation），结合知识检索的AI回答方式 |
| LLM | 大语言模型（Large Language Model） |
| Prompt | 给AI的指令/提示词 |
| 向量数据库 | 存储文本向量化后的数据库，用于语义相似度检索 |
| 知识库 | 结构化/非结构化的知识存储，供AI检索使用 |
| 编排引擎 | 协调多个智能体按流程执行的调度系统 |
| 人工审核节点 | 流程中需要人工确认才能继续的关键检查点 |
| 工作流 | 按照既定顺序和规则执行的一系列任务 |
